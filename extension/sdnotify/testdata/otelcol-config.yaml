extensions:
  sdnotifyextension:

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 127.0.0.1:4317

exporters:
  debug: {}

service:
  extensions: [sdnotifyextension]
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug]
